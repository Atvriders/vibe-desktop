services:
  vibe-desktop:
    build: .
    ports:
      - "3000:3000"
    environment:
      # Paste your Anthropic API key here (replace the placeholder).
      # This file is in a PUBLIC repo — do NOT commit a real key.
      ANTHROPIC_API_KEY: "sk-ant-REPLACE_WITH_YOUR_KEY"
    restart: unless-stopped
